spring.application.name=armoire
# server.address=127.0.0.1
# server.port=9090

# ==========================
# JWT Secret Key
# ==========================
jwt.secret=THISISMYSECURESECRETKEYFORJWTECOMMERCEBACKEND123456

# ==========================
# Database
# ==========================
spring.datasource.url=jdbc:postgresql://ep-mute-meadow-a1um972m-pooler.ap-southeast-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_Q5fnBeSO7Isj&sslmode=require&channelBinding=require
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=neondb_owner
spring.datasource.password=npg_Q5fnBeSO7Isj


# ==========================
# JPA & Hibernate
# ==========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ==========================
# Mail Settings
# ==========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bunnyking828@gmail.com
spring.mail.password=rwmc rraa adwm ukcq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# ==========================
# JWT Token Expiry Settings
# ==========================
jwt.access-token-expiration=900000  
jwt.refresh-token-expiration=604800000
jwt.cookie.secure=false

# ==========================
# App Settings
# ==========================
app.frontend-url=http://localhost:3000
app.verification-token-expiration=86400000

# Pagination defaults
spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100


# AWS S3 Configuration
aws.access.key=AKIASOLKTCZLEWGLZYRX
aws.secret.key=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
aws.region=eu-north-1
aws.s3.bucket=nala-armorie-storage

# CloudFront (Optional - leave empty if not using CDN)
aws.cloudfront.domain=


# Multipart Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


# Order Configuration
order.cancellation.hours=24
order.return.days=7

# Inventory Configuration
inventory.low-stock-threshold=10

# Shipping Cost Price
shipping.cost.price=100

# Shipping Threshold (free shipping if cart exceeds this amount)
shipping.threshold=1000

# GST
gst.rate=18

# Razorpay Configuration
razorpay.key.id=rzp_test_S4z4JEgdwEl5wN
razorpay.key.secret=q23JmY78mvGAV35LSLs2yxIR
razorpay.currency=INR

# Admin Email (for low stock alerts)
admin.email=bunnyking828@gmail.com


# ==========================
# SMS Provider (Twilio)
# ==========================
twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.phone-number=${TWILIO_PHONE_NUMBER:}

management.endpoint.health.show-details=always

# ==========================
# Redis Configuration
# ==========================
spring.data.redis.host=${REDIS_HOST:redis}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=60000
spring.data.redis.jedis.pool.max-active=8
spring.data.redis.jedis.pool.max-idle=8
spring.data.redis.jedis.pool.min-idle=0

# ===============================
# Resilience4j RateLimiter - Admin writes
# ===============================
resilience4j.ratelimiter.instances.admin-write.limit-for-period=20
resilience4j.ratelimiter.instances.admin-write.limit-refresh-period=1m
resilience4j.ratelimiter.instances.admin-write.timeout-duration=0


spring.servlet.multipart.enabled=true


